version: 0.0
os: linux
files:
 - source: .
   destination: /home/ec2-user/tweet-streamer
hooks:
   AfterInstall:
     - location: scripts/stop_app
       timeout: 300
   ApplicationStart:
     - location: scripts/start_app
       timeout: 300

